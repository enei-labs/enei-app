overwrite: true
schema: 'http://localhost:3004/graphql'
generates:
  core/graphql/types.ts:
    plugins:
      - 'typescript'
      # - 'typescript-operations'
      # - 'typescript-graphql-request'
    config:
      maybeValue: T | undefined
      inputMaybeValue: T | undefined
  core/graphql/possibleTypes.ts:
    plugins:
      - fragment-matcher
