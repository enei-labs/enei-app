import { Button } from "@mui/material";
import AddIcon from "@mui/icons-material/AddCircleOutlineOutlined";

const AddTransferDocumentBtn = () => {
  return (
    <Button startIcon={<AddIcon />} onClick={() => {}}>
      新增轉供合約
    </Button>
  );
};

export default AddTransferDocumentBtn;
